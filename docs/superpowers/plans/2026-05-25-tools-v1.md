# 工具功能 v1 Implementation Plan

> **归档：** 实施 checklist（2026-05-25）。**现网规格：** [features/tools-mcp-skills.md](../../features/tools-mcp-skills.md)

**Goal:** 交付统一工具工作台（分类 + 参数 schema + HTTP 自定义工具 + 内置注册表），对齐已批准设计规格 `docs/superpowers/specs/2026-05-25-tools-design.md`。

**Architecture:** 内置工具由代码注册表维护；自定义 HTTP 工具存 `tool_tools` 表，扩展 slug/category/parameters 等列；catalog API 合并 builtin/custom/mcp；HTTP 执行升级参数校验与模板 URL；前端复用 MCP/分类页模式。

**Tech Stack:** FastAPI, SQLAlchemy, Alembic, Pydantic v2, httpx, LangChain StructuredTool, Next.js, React, TypeScript

**Spec:** [2026-05-25-tools-design.md](../specs/2026-05-25-tools-design.md)

---

## 文件结构

| 文件 | 职责 |
|------|------|
| `backend/alembic/versions/006_tool_v2_fields.py` | DB migration |
| `backend/app/tenant/tools/models.py` | Tool 模型扩展 |
| `backend/app/tenant/tools/builtin_registry.py` | 内置工具注册表（新建） |
| `backend/app/tenant/tools/parameters.py` | 参数 schema 校验 + Pydantic 动态模型（新建） |
| `backend/app/tenant/tools/invoke.py` | 执行逻辑升级 |
| `backend/app/tenant/tools/schemas/tools.py` | API schema |
| `backend/app/tenant/tools/services/tools.py` | CRUD + catalog |
| `backend/app/tenant/tools/views/tools.py` | 路由 |
| `backend/app/common/url_security.py` | 出站 URL 校验（从 MCP 提取，新建） |
| `backend/app/models/category.py` | CategoryDomain.TOOL |
| `backend/scripts/seed/categories.py` | tool 默认分类 |
| `backend/app/integrations/langchain/tools.py` | 动态 StructuredTool |
| `backend/tests/test_tools_*.py` | 测试 |
| `frontend/lib/types.ts` | 类型 |
| `frontend/lib/api.ts` | API 客户端 |
| `frontend/lib/tool-labels.ts` | 来源/格式化 helper（新建） |
| `frontend/components/tool/*.tsx` | UI 组件（新建） |
| `frontend/app/workbench/tools/page.tsx` | 页面重写 |

---

### Task 1: 出站 URL 校验共用模块

**Files:**
- Create: `backend/app/common/url_security.py`
- Modify: `backend/app/tenant/mcp/security.py`
- Test: `backend/tests/test_url_security.py`

- [ ] **Step 1: Write the failing test**

```python
# backend/tests/test_url_security.py
import pytest
from app.common.exceptions import BadRequestError
from app.common.url_security import validate_outbound_url


def test_rejects_localhost():
    with pytest.raises(BadRequestError, match="本机"):
        validate_outbound_url("http://127.0.0.1/api")


def test_accepts_public_https():
    assert validate_outbound_url("https://api.example.com/v1") == "https://api.example.com/v1"
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd backend && pytest tests/test_url_security.py -v`  
Expected: FAIL — module not found

- [ ] **Step 3: Implement url_security + refactor mcp/security.py**

```python
# backend/app/common/url_security.py
"""出站 HTTP URL 校验（工具、MCP 共用）。"""

import ipaddress
from urllib.parse import urlparse

from app.common.exceptions import BadRequestError
from app.core.config import get_settings


def validate_outbound_url(url: str) -> str:
    raw = (url or "").strip()
    if not raw:
        raise BadRequestError("URL 不能为空")
    parsed = urlparse(raw)
    if parsed.scheme not in ("http", "https"):
        raise BadRequestError("URL 仅支持 http 或 https")
    if not parsed.hostname:
        raise BadRequestError("URL 无效")
    if not get_settings().mcp_allow_private_hosts:
        _reject_unsafe_host(parsed.hostname)
    return raw


def _reject_unsafe_host(host: str) -> None:
    lowered = host.lower()
    if lowered in ("localhost", "127.0.0.1", "::1", "0.0.0.0"):
        raise BadRequestError("不允许连接本机地址")
    try:
        addr = ipaddress.ip_address(host)
    except ValueError:
        return
    if addr.is_private or addr.is_loopback or addr.is_link_local or addr.is_reserved or addr.is_multicast:
        raise BadRequestError("不允许连接内网或保留地址段")
```

```python
# backend/app/tenant/mcp/security.py — 改为 re-export
from app.common.url_security import validate_outbound_url as validate_mcp_endpoint_url
```

- [ ] **Step 4: Run test — PASS**

Run: `cd backend && pytest tests/test_url_security.py -v`

---

### Task 2: 参数 schema 校验模块

**Files:**
- Create: `backend/app/tenant/tools/parameters.py`
- Test: `backend/tests/test_tools_parameters.py`

- [ ] **Step 1: Write failing tests**

```python
# backend/tests/test_tools_parameters.py
import pytest
from app.common.exceptions import BadRequestError
from app.tenant.tools.parameters import validate_tool_params, parameters_to_pydantic


SAMPLE = [
    {"name": "city", "type": "string", "description": "城市", "required": True},
    {"name": "unit", "type": "string", "enum": ["c", "f"], "required": False, "default": "c"},
]


def test_validate_ok():
    validate_tool_params(SAMPLE, {"city": "Beijing"})


def test_validate_missing_required():
    with pytest.raises(BadRequestError, match="city"):
        validate_tool_params(SAMPLE, {})


def test_pydantic_model():
    Model = parameters_to_pydantic(SAMPLE)
    m = Model(city="x")
    assert m.city == "x"
    assert m.unit == "c"
```

- [ ] **Step 2: Run — FAIL**

- [ ] **Step 3: Implement parameters.py**

```python
# backend/app/tenant/tools/parameters.py
"""工具输入参数 schema 校验与动态 Pydantic 模型。"""

from __future__ import annotations

from typing import Any

from pydantic import BaseModel, Field, create_model

from app.common.exceptions import BadRequestError

_ALLOWED_TYPES = {"string", "number", "integer", "boolean"}


def normalize_parameters(raw: list | None) -> list[dict]:
    if not raw:
        return []
    names: set[str] = set()
    out: list[dict] = []
    for i, p in enumerate(raw):
        if not isinstance(p, dict):
            raise BadRequestError(f"parameters[{i}] 必须是对象")
        name = str(p.get("name", "")).strip()
        if not name or not name.replace("_", "").isalnum() or not name[0].isalpha():
            raise BadRequestError(f"parameters[{i}].name 无效: {name!r}")
        if name in names:
            raise BadRequestError(f"参数名重复: {name}")
        names.add(name)
        ptype = str(p.get("type", "string"))
        if ptype not in _ALLOWED_TYPES:
            raise BadRequestError(f"parameters[{i}].type 不支持: {ptype}")
        out.append({**p, "name": name, "type": ptype})
    return out


def validate_tool_params(schema: list[dict], params: dict[str, Any]) -> dict[str, Any]:
    schema = normalize_parameters(schema)
    coerced: dict[str, Any] = {}
    for p in schema:
        name = p["name"]
        required = bool(p.get("required"))
        if name in params and params[name] is not None:
            val = params[name]
        elif "default" in p:
            val = p["default"]
        elif required:
            raise BadRequestError(f"缺少必填参数: {name}")
        else:
            continue
        coerced[name] = _coerce(val, p)
    return coerced


def _coerce(val: Any, spec: dict) -> Any:
    t = spec["type"]
    enum = spec.get("enum")
    if t == "string":
        val = str(val)
    elif t == "integer":
        val = int(val)
    elif t == "number":
        val = float(val)
    elif t == "boolean":
        val = bool(val) if not isinstance(val, str) else val.lower() in ("1", "true", "yes")
    if enum and val not in enum:
        raise BadRequestError(f"参数 {spec['name']} 必须是 {enum} 之一")
    return val


def parameters_to_pydantic(schema: list[dict]) -> type[BaseModel]:
    schema = normalize_parameters(schema)
    fields: dict[str, Any] = {}
    for p in schema:
        py_type = {"string": str, "integer": int, "number": float, "boolean": bool}[p["type"]]
        default = ... if p.get("required") else p.get("default", None)
        fields[p["name"]] = (py_type, Field(default=default, description=p.get("description")))
    return create_model("ToolParams", **fields)
```

- [ ] **Step 4: Run — PASS**

---

### Task 3: 内置工具注册表 + get_current_datetime

**Files:**
- Create: `backend/app/tenant/tools/builtin_registry.py`
- Modify: `backend/app/tenant/tools/invoke.py`
- Modify: `backend/app/tenant/tools/services/tools.py`
- Test: `backend/tests/test_tools_invoke.py`

- [ ] **Step 1: Write failing test for datetime**

```python
# backend/tests/test_tools_invoke.py
import pytest
from app.tenant.tools.invoke import invoke_builtin


@pytest.mark.asyncio
async def test_get_current_datetime(db, tenant_ctx):
    out = await invoke_builtin("get_current_datetime", {}, db=db, ctx=tenant_ctx)
    assert "datetime" in out
    assert "T" in out["datetime"]
```

- [ ] **Step 2: Create builtin_registry.py**

```python
# backend/app/tenant/tools/builtin_registry.py
BUILTIN_REGISTRY: list[dict] = [
    {
        "slug": "calculator",
        "name": "计算器",
        "description": "安全计算数学表达式",
        "category_slug": "general",
        "version": "1.0.0",
        "require_confirmation": False,
        "parameters": [
            {"name": "expression", "type": "string", "description": "数学表达式", "required": True},
        ],
    },
    {
        "slug": "http_request",
        "name": "HTTP 请求",
        "description": "发起 HTTP 请求",
        "category_slug": "integration",
        "version": "1.0.0",
        "require_confirmation": False,
        "parameters": [
            {"name": "url", "type": "string", "required": True},
            {"name": "method", "type": "string", "required": False, "default": "GET"},
        ],
    },
    {
        "slug": "knowledge_search",
        "name": "知识库检索",
        "description": "在指定知识库中语义检索",
        "category_slug": "data",
        "version": "1.0.0",
        "require_confirmation": False,
        "parameters": [
            {"name": "query", "type": "string", "required": True},
            {"name": "kb_id", "type": "string", "required": True},
            {"name": "limit", "type": "integer", "required": False, "default": 5},
        ],
    },
    {
        "slug": "get_current_datetime",
        "name": "获取当前时间",
        "description": "获取当前的日期时间",
        "category_slug": "general",
        "version": "1.0.0",
        "require_confirmation": False,
        "parameters": [
            {"name": "timezone", "type": "string", "description": "IANA 时区，默认 UTC", "required": False},
        ],
    },
]

BUILTIN_SLUGS = {t["slug"] for t in BUILTIN_REGISTRY}


def get_builtin(slug: str) -> dict | None:
    for t in BUILTIN_REGISTRY:
        if t["slug"] == slug:
            return t
    return None
```

- [ ] **Step 3: Add invoke branch in invoke.py**

```python
# 在 invoke_builtin 末尾前添加：
    if name == "get_current_datetime":
        from datetime import datetime, timezone
        from zoneinfo import ZoneInfo

        tz_name = params.get("timezone") or "UTC"
        try:
            tz = ZoneInfo(str(tz_name))
        except Exception:
            raise BadRequestError(f"无效时区: {tz_name}")
        now = datetime.now(tz)
        return {"datetime": now.isoformat(), "timezone": tz_name}
```

- [ ] **Step 4: Update invoke_tool_by_name to use BUILTIN_SLUGS**

Replace `builtin_names = {"calculator", ...}` with `from app.tenant.tools.builtin_registry import BUILTIN_SLUGS` and `if name in BUILTIN_SLUGS`.

- [ ] **Step 5: Run test — PASS**

---

### Task 4: DB Migration + Model

**Files:**
- Create: `backend/alembic/versions/006_tool_v2_fields.py`
- Modify: `backend/app/tenant/tools/models.py`

- [ ] **Step 1: Update models.py**

```python
class ToolType(str, enum.Enum):
    HTTP = "http"
    SCRIPT = "script"  # v1 禁创建


class Tool(...):
  slug: Mapped[str] = mapped_column(String(64), nullable=False)
  category_id: Mapped[uuid.UUID | None] = mapped_column(UUID(as_uuid=True), nullable=True)
  version: Mapped[str] = mapped_column(String(16), default="1.0.0", nullable=False)
  require_confirmation: Mapped[bool] = mapped_column(Boolean, default=False, nullable=False)
  parameters: Mapped[list] = mapped_column(JSONB, default=list, nullable=False)
  # 更新 __table_args__ unique: tenant_id + slug
```

- [ ] **Step 2: Write migration 006**

- 添加列 slug, category_id, version, require_confirmation, parameters
- 回填：`UPDATE tool_tools SET slug = lower(regexp_replace(name, '[^a-zA-Z0-9]+', '_', 'g'))`
- 删除旧 unique index `un_tool_tools_tenant_id_name`，新建 `un_tool_tools_tenant_id_slug`
- 若 enum 含 builtin，迁移改为 http

- [ ] **Step 3: Run migration**

Run: `cd backend && alembic upgrade head`

---

### Task 5: CategoryDomain.TOOL + Seed

**Files:**
- Modify: `backend/app/models/category.py`
- Modify: `backend/scripts/seed/categories.py`
- Modify: `frontend/lib/types.ts` — `CategoryDomain` 加 `"tool"`

- [ ] **Step 1: Add TOOL to CategoryDomain enum**

```python
class CategoryDomain(str, enum.Enum):
    AGENT = "agent"
    PROMPT = "prompt"
    SKILL = "skill"
    TOOL = "tool"
```

- [ ] **Step 2: Add DEFAULT_TOOL_CATEGORIES and seed loop**（general/integration/data/uncategorized）

- [ ] **Step 3: Run seed**

Run: `cd backend && python -m scripts.db_ops seed`（或项目现有 seed 命令）

---

### Task 6: Schemas + ToolsService

**Files:**
- Modify: `backend/app/tenant/tools/schemas/tools.py`
- Modify: `backend/app/tenant/tools/services/tools.py`
- Modify: `backend/app/tenant/tools/views/tools.py`

- [ ] **Step 1: Extend schemas**

```python
import re
SLUG_RE = re.compile(r"^[a-z][a-z0-9_]{0,62}$")

class ToolParameterSpec(BaseModel):
    name: str
    type: str = "string"
    description: str | None = None
    required: bool = False
    default: Any = None
    enum: list | None = None

class ToolCreate(BaseModel):
    slug: str
    name: str
    description: str | None = None
    tool_type: ToolType = ToolType.HTTP
    category_id: UUID | None = None
    version: str = "1.0.0"
    require_confirmation: bool = False
    parameters: list[ToolParameterSpec] = Field(default_factory=list)
    config: dict = Field(default_factory=dict)

    @field_validator("slug")
    @classmethod
    def validate_slug(cls, v: str) -> str:
        s = v.strip()
        if not SLUG_RE.match(s):
            raise ValueError("slug 须为小写字母、数字、下划线")
        return s
```

- [ ] **Step 2: ToolOut / ToolCatalogItem 扩展** — 含 slug, category_id, category_name, parameters, version, updated_at

- [ ] **Step 3: ToolsService.create_tool**

- 拒绝 `tool_type=SCRIPT`
- 拒绝 slug 在 BUILTIN_SLUGS
- `normalize_parameters(body.parameters)`
- `validate_outbound_url(config["url"])` when HTTP
- 查重 slug

- [ ] **Step 4: list_catalog 增强**

- source filter query param
- category_id filter
- builtin 从 BUILTIN_REGISTRY
- custom join category name
- 按 slug 作为 catalog name（兼容 invoke）

- [ ] **Step 5: list_tools 支持 category_id query**

- [ ] **Step 6: views 加 query params**

```python
@router.get("/catalog")
async def tool_catalog(source: str | None = None, category_id: UUID | None = None, ...):
```

---

### Task 7: HTTP 执行升级

**Files:**
- Modify: `backend/app/tenant/tools/invoke.py`
- Test: `backend/tests/test_tools_invoke.py`

- [ ] **Step 1: Rewrite invoke_custom_http**

```python
def _apply_template(template: str, params: dict) -> str:
    out = template
    for k, v in params.items():
        out = out.replace(f"{{{{{k}}}}}", str(v))
    return out


async def invoke_custom_http(tool: Tool, params: dict) -> dict:
    validated = validate_tool_params(tool.parameters or [], params)
    cfg = tool.config or {}
    url = _apply_template(str(cfg.get("url", "")), validated)
    if not url:
        raise BadRequestError("HTTP 工具未配置 url")
    validate_outbound_url(url)
    method = str(cfg.get("method", "POST")).upper()
    headers = {k: _apply_template(str(v), validated) for k, v in (cfg.get("headers") or {}).items()}
    timeout = float(cfg.get("timeout_sec", 15))
    async with httpx.AsyncClient(timeout=timeout) as client:
        if method == "GET":
            resp = await client.get(url, params=validated, headers=headers)
        else:
            body_mode = cfg.get("body_mode", "json")
            kwargs = {"headers": headers}
            if body_mode == "json":
                kwargs["json"] = validated
            resp = await client.request(method, url, **kwargs)
    text = resp.text[:4000]
    result: dict = {"status_code": resp.status_code, "body": text}
    path = cfg.get("response_path")
    if path and resp.headers.get("content-type", "").startswith("application/json"):
        try:
            data = resp.json()
            for part in str(path).split("."):
                data = data[part]
            result["extracted"] = data
        except Exception:
            pass
    return result
```

- [ ] **Step 2: invoke_tool_by_name 优先 slug 匹配**

- [ ] **Step 3: Add tests for GET query + template URL — PASS**

---

### Task 8: LangChain 动态工具

**Files:**
- Modify: `backend/app/integrations/langchain/tools.py`

- [ ] **Step 1: Refactor builtins to use BUILTIN_REGISTRY**

- [ ] **Step 2: Add make_custom_http_tools(db, ctx) async**

```python
async def load_tenant_http_tools(db, ctx) -> list[StructuredTool]:
    # select active HTTP tools for tenant
    # for each: parameters_to_pydantic + invoke closure calling invoke_custom_http
```

- [ ] **Step 3: Wire into agent runner**（查找 `get_platform_tools` 调用点，合并 tenant tools）

---

### Task 9: 前端类型与 API

**Files:**
- Modify: `frontend/lib/types.ts`
- Modify: `frontend/lib/api.ts`
- Create: `frontend/lib/tool-labels.ts`

- [ ] **Step 1: Add types**

```typescript
export interface ToolParameterSpec {
  name: string;
  type: "string" | "number" | "integer" | "boolean";
  description?: string | null;
  required?: boolean;
  default?: unknown;
  enum?: string[];
}

export interface ToolCatalogItem {
  source: "builtin" | "custom" | "mcp";
  slug: string;
  name: string;
  description?: string | null;
  category_id?: string | null;
  category_name?: string | null;
  parameters?: ToolParameterSpec[];
  version?: string;
  tool_id?: string | null;
  mcp_service_id?: string | null;
  mcp_service_name?: string | null;
  updated_at?: string | null;
}

export interface CustomTool {
  id: string;
  slug: string;
  name: string;
  // ... extended fields
}
```

- [ ] **Step 2: Update api.ts**

```typescript
listToolCatalog: (source?: string, categoryId?: string) => ...
createCustomTool: (body: ToolCreatePayload) => post<CustomTool>("/tools", body)
updateCustomTool: (id: string, body: Partial<ToolCreatePayload>) => patch(...)
```

- [ ] **Step 3: tool-labels.ts** — sourceLabel, formatToolUpdatedAt（复用 mcp-labels 模式）

---

### Task 10: 前端组件

**Files:**
- Create: `frontend/components/tool/ToolCard.tsx`
- Create: `frontend/components/tool/ToolCreateDialog.tsx`
- Create: `frontend/components/tool/ToolParameterEditor.tsx`
- Create: `frontend/components/tool/ToolTestDialog.tsx`

- [ ] **Step 1: ToolParameterEditor** — 动态行：name/type/description/required/enum/default；+ 添加参数

- [ ] **Step 2: ToolCreateDialog** — 参考 McpServiceDialog + 设计稿字段；HTTP 区：url/method/headers textarea(JSON)；info banner；无代码编辑器

- [ ] **Step 3: ToolTestDialog** — 根据 parameters 渲染 input；执行 api.invokeTool(slug, values, toolId)

- [ ] **Step 4: ToolCard** — 参考 McpServiceCard：🔧 图标、slug 作 subtitle、badge（内置/自定义/MCP + 分类名）、··· 菜单（编辑/删除/试调用；内置仅试调用）

---

### Task 11: 工具页重写

**Files:**
- Modify: `frontend/app/workbench/tools/page.tsx`

- [ ] **Step 1: Replace dual-tab with source tabs**

```typescript
type SourceTab = "" | "builtin" | "custom";
const [sourceTab, setSourceTab] = useState<SourceTab>("");
const cat = useCategoryTabs("tool");
```

- [ ] **Step 2: Single ResourceListLayout grid**

- 全部：catalog filtered by sourceTab + category + search
- 自定义 Tab：AddResourceCard + custom tools from catalog or paged list
- CategoryManageDialog

- [ ] **Step 3: Wire create/edit/delete/test dialogs**

- [ ] **Step 4: Manual smoke test**

1. 打开 `/workbench/tools`
2. 看到内置 `get_current_datetime`
3. 创建 HTTP 工具带 2 个参数
4. 试调用成功

---

### Task 12: 集成测试与文档

- [ ] **Step 1: Run backend tests**

Run: `cd backend && pytest tests/test_tools_parameters.py tests/test_tools_invoke.py tests/test_url_security.py -v`

- [ ] **Step 2: Run frontend typecheck**

Run: `cd frontend && npx tsc --noEmit`

- [ ] **Step 3: Update technical-design.md** — 工具章节补充 v1 字段（可选，若用户未要求可跳过）

---

## Plan Self-Review

| 规格章节 | 对应 Task |
|----------|-----------|
| 数据模型 | Task 4 |
| 分类 | Task 5 |
| 内置注册表 | Task 3 |
| API | Task 6 |
| HTTP 执行 | Task 7 |
| Agent 集成 | Task 8 |
| 前端 UI | Task 9–11 |
| 测试 | Task 1–3, 7, 12 |
| v2 script 预留 | Task 6 拒绝 SCRIPT 创建；UI banner Task 10 |

无 TBD/placeholder。类型名 `ToolParameterSpec`、`ToolCatalogItem` 前后一致。
