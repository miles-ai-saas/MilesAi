/** 智能体表单共享入口（兼容 import 路径）。 */

export { AGENT_FORM_STEPS, emptyAgentForm, type AgentFormValues } from "@/features/agents/lib/agent-form-types";
export { agentToFormValues, buildAgentConfig, formatAgentCode } from "@/features/agents/lib/agent-form-config";
