/** Tailwind font-sans 栈（设计 token，见 design.md）。 */

/** 供 Tailwind 与 CSS 共用的 sans 字体栈（优先系统自带中文字体） */
export const fontFamilySans = ["PingFang SC", "Microsoft YaHei", "Segoe UI", "system-ui", "-apple-system", "sans-serif"] as const;
